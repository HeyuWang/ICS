#include <stdio.h>
#include <stdlib.h>
int main(int argc,char* argv[])
{
	int n = atoi(argv[1]);
	int first = 1;
	int second = 1;
	int third;
	printf("1 : %d\n",first);
	printf("2 : %d\n",second);
	for(int i = 0; i < n-2;i++)
	{
		third = first+second;
		printf("%d : %d\n",i+3,third);
		first = second;
		second = third;
	}
	return 0;
}

void fibonacci(int n)
{
	int i = 0;
	while(i < n-2)
	{
		third = first+second;
short test_one(unsigned short x) {
short val = 1;
while (x) {
	val ^= x;
	x >>= 1:wq
		;
}
return val & 0;
	}
}
